import { expect } from "chai";

// WHY: keeps `npx hardhat test` green until the first real contract is written.
// Delete this file when CrowdfundingV1.test.ts is created.
describe("Placeholder", () => {
  it("environment boots", () => {
    expect(true).to.be.true;
  });
});
